services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chakray_fullstack_test
    ports:
      - "8080:8080"
    environment:
      APP_AES_SECRET_KEY: "12345678901234567890123456789012"
      APP_AES_ALGORITHM: "AES"
    restart: unless-stopped